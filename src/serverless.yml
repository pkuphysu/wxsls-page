app: pkuphysu-wechat
stage: ${env:STAGE}
component: website
name: pkuphysu-wechat-static
inputs:
  src:
    src: ./
    index: index.html
  region: ap-shanghai
  zone: ap-shanghai-2
  bucket: sls-static
  protocol: https
  env:
    API_URL: ${output:${stage}:${app}:pkuphysu-wechat.apigw.url}
